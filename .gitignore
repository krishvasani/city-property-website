# build output
dist/
.astro/

# deps
node_modules/

# env (never commit real keys)
.env
.env.production
.env.local

# os / editor
.DS_Store
*.log
npm-debug.log*

# sanity
sanity/dist/
.sanity/

# Local Netlify folder
.netlify
seed.ndjson
localities.ndjson
.cache/

# Source documents for the Adani Shantigram project pages (Word/PDF brochures,
# cost sheets, reels). Read locally by the extraction scripts only — never
# shipped. The extracted facts live in src/content/projects/shantigram/.
**/Adani_Shantigram_Reel_Series/
